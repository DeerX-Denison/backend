import 'ts-jest';

describe('Testing listingDataChanged', () => {
	it.todo('return true if current and new listing data changed');
	it.todo('return false if current and new listing data remains the same');
});
